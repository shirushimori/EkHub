# Server-side (set in Vercel dashboard under Settings → Environment Variables)
# TMDB_API_KEY=your_tmdb_v4_api_key

# Client-side (optional, defaults to same-origin /api/*)
# VITE_API_BASE_URL=https://your-app.vercel.app
